apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "async-long-request-proxy.fullname" . }}
  labels:
    {{- include "async-long-request-proxy.labels" . | nindent 4 }}
data:
  NODE_ENV: {{ .Values.config.nodeEnv | quote }}
  PORT: {{ .Values.config.port | quote }}
